## General

The IconLink currently takes 100% of the parent's width at all times. The color is also fixed as var(--contrastColor), with
var(--primaryColor) as the hover color and a variant of 20% opacity of same primary color as a hover background.
